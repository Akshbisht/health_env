[build-system]
requires = ["setuptools >= 45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "health-env"
version = "1.0.0"
description = "Pediatric triage RL environment as a Hugging Face Space"
